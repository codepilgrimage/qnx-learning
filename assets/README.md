# Assets

- `images/` — diagrams exported as PNG/SVG, referenced from `/docs/`
- `screenshots/` — IDE/terminal screenshots used in walkthroughs
- `gifs/` — short screen recordings (debugging sessions, demos)

Keep filenames descriptive and matched to the chapter that uses them, e.g.
`02-process-thread-pidin-output.png`.
